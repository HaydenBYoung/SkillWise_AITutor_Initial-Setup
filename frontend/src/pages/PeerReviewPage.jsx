// Peer review page with real API integration
import PeerReview from './PeerReview';
import DashboardLayout from '../components/common/DashboardLayout';

const PeerReviewPage = () => {
  return (
    <DashboardLayout>
      <PeerReview />
    </DashboardLayout>
  );
};

export default PeerReviewPage;
