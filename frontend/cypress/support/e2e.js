// Cypress support file (minimal)
// This file is loaded before test files; keep minimal for now.

// Optionally import custom commands from ./commands
try {
  require('./commands');
} catch (e) {
  // no-op if commands file doesn't exist
}
